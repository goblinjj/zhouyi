# Cloudflare Pages 自定义响应头
#
# 全站 no-store：让浏览器(含 bfcache / 安卓 WebView)每次都拉取最新内容，
# 避免缓存的旧 HTML 引用到新部署已删除的内容哈希资源(JS/CSS)，
# 导致 404 → 整页丢失样式。
# 说明：SPA 一次会话只整页加载一次(内部路由为客户端跳转)，故对性能影响很小。
/*
  Cache-Control: no-store, must-revalidate
